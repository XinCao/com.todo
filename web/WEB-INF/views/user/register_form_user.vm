#include("shared/macro_ui.vm")
#include("shared/header.vm")
<style type="text/css">
    body {
      padding-top: 40px;
      padding-bottom: 40px;
      background-color: #f5f5f5;
    }
    .form-signin {
      max-width: 300px;
      padding: 19px 29px 29px;
      margin: 0 auto 20px;
      background-color: #fff;
      border: 1px solid #e5e5e5;
      -webkit-border-radius: 5px;
         -moz-border-radius: 5px;
              border-radius: 5px;
      -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
         -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
              box-shadow: 0 1px 2px rgba(0,0,0,.05);
    }
    .form-signin .form-signin-heading,
    .form-signin .checkbox {
      margin-bottom: 10px;
    }
    .form-signin input[type="text"],
    .form-signin input[type="password"] {
      font-size: 16px;
      height: auto;
      margin-bottom: 15px;
      padding: 7px 9px;
    }
</style>
<form id="register_form" class="form-signin" action="/user/do_register_user" method="post">
    <h2 class="form-signin-heading">请注册</h2>
    #input("User.account", "text","帐号")
    #input("User.email", "text", "邮箱")
    #input("User.passwd", "password", "密码")
    #input("User.againPasswd", "password", "确认密码")
    <div class="row-fluid">
        <button class="btn btn-large btn-block btn-primary" type="submit">注册</button>
    </div>
</form>
<script type="text/javascript">
    $(document).ready(function(){
        $("#register_form").validate({
            rules: {
                account: {
                    required: true,
                    rangelength:[6,25]
                },
                email: {
                    required: true,
                    email: true
                },
                passwd: {
                    required: true
                },
                againPasswd: {
                    required: true,
                    equalTo: "#password"
                }
            }
        });
    });
</script>
#include("shared/footer.vm")